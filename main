#!/bin/bash


if [[ -e /tmp/dnscheck ]]; then

    clear
    /etc/dns/dns-changer
    
else

    sudo cp /etc/resolv.conf /etc/dns/backup.conf
    echo done! >> /tmp/dnscheck
    clear
    /etc/dns/dns-changer

fi